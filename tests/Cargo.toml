[package]
name = "par-tests"
version = "0.1.0"
edition = "2021"
publish = false

[dev-dependencies]
par-core = { path = "../core", features = ["testing"] }
proptest = "1.5"
tempfile = "3.10"
assert_cmd = "2.0"
predicates = "3.1"
